# openjdk11-reference-binaries
Not to be confused with openjdk11-binaries (which are AdoptOpenJDK's official binary release).  These are pure unaltered builds from the OpenJDK mercurial jdk11u code stream.
